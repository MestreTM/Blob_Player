function loadVideoPlayer() {    var videoLink = document.getElementById('video-container').getAttribute('data-video-link');    fetch('player.php?link=' + encodeURIComponent(videoLink))        .then(response => response.text())        .then(html => {            var blob = new Blob([html], { type: 'text/html' });            var blobUrl = URL.createObjectURL(blob);            var iframe = document.createElement('iframe');            iframe.src = blobUrl;            iframe.width = '100%';            iframe.height = '500px';            iframe.frameBorder = '0';            document.getElementById('video-container').appendChild(iframe);        })        .catch(error => {            console.error('Error loading the player:', error);        });}window.onload = loadVideoPlayer;